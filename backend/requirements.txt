# Core dependencies
anthropic>=0.40.0
python-dotenv>=1.0.0

# Web API (simple deployment)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6  # Required for file uploads

# Database (usage tracking)
sqlmodel>=0.0.14

# Chat and auth
PyJWT>=2.8.0                # JWT token verification (WordPress auth)
slowapi>=0.1.9              # FastAPI-compatible rate limiting

# Web crawler and analysis
playwright>=1.40.0  # For web crawler and screenshot capture
Pillow>=10.0.0  # For screenshot compression and image processing
requests>=2.32.0  # For Figma API integration and Google Drive downloads

# Book manuscript processing
pypdf[image]>=3.1.0  # PDF text and image extraction (3.1+ required for page.images API; [image] pulls in Pillow)

# Development/testing (optional)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0

# Copy this file to .env.test and fill in your credentials.
# .env.test is gitignored — never commit real credentials.

# Railway backend URL (no trailing slash)
ANALYZER_URL=https://your-app.railway.app

# WordPress JWT token (get one by logging in to the WP site and copying
# the token from the browser's network tab or localStorage)
ANALYZER_TOKEN=eyJ...
